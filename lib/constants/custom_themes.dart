import 'package:flutter/material.dart';

var appBarTheme = AppBar();

class CustomThemes {
  TextStyle appBarStyleTheme() {
    return const TextStyle(
      color: Colors.teal,
      fontSize: 18.0,
      fontWeight: FontWeight.w500,
    );
  }
}
